import { EquipmentTypeModel } from './equipment-type.model';

describe('EquipmentTypeModel', () => {
  it('should create an instance', () => {
    expect(new EquipmentTypeModel()).toBeTruthy();
  });
});
