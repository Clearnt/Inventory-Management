import { EquipmentStateModel } from './equipment-state.model';

describe('EquipmentStateModel', () => {
  it('should create an instance', () => {
    expect(new EquipmentStateModel()).toBeTruthy();
  });
});
