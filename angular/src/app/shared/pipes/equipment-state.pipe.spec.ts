import { EquipmentStatePipe } from './equipment-state.pipe';

describe('EquipmentStatePipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentStatePipe();
    expect(pipe).toBeTruthy();
  });
});
