import { DepartametPipe } from './departamet.pipe';

describe('DepartametPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartametPipe();
    expect(pipe).toBeTruthy();
  });
});
